[gd_scene load_steps=5 format=3 uid="uid://bpjv8g6d315k8"]

[ext_resource type="PackedScene" uid="uid://di2iwpjhc7xnl" path="res://scenes/ui/popup_label/popup_label.tscn" id="1_xy2q2"]
[ext_resource type="Texture2D" uid="uid://b2n1lhfopw4yq" path="res://assets/textures/icons/gold_coins_icon.png" id="2_t33iy"]

[sub_resource type="Shader" id="Shader_vxi4y"]
code = "shader_type canvas_item;

uniform vec4 outline_color : source_color = vec4(1.0);
uniform vec3 modulate : source_color = vec3(1.0);
uniform float alpha : hint_range(0.0, 1.0, 0.1) = 1.0;
uniform float width : hint_range(0, 10) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // diamond, circle, square
uniform bool inside = false;
uniform bool add_margins = true; // only useful when inside is false

void vertex() {
	if (add_margins) {
		VERTEX += (UV * 2.0 - 1.0) * width;
	}
}

bool hasContraryNeighbour(vec2 uv, vec2 texture_pixel_size, sampler2D texture) {
	for (float i = -ceil(width); i <= ceil(width); i++) {
		float x = abs(i) > width ? width * sign(i) : i;
		float offset;
		
		if (pattern == 0) {
			offset = width - abs(x);
		} else if (pattern == 1) {
			offset = floor(sqrt(pow(width + 0.5, 2) - x * x));
		} else if (pattern == 2) {
			offset = width;
		}
		
		for (float j = -ceil(offset); j <= ceil(offset); j++) {
			float y = abs(j) > offset ? offset * sign(j) : j;
			vec2 xy = uv + texture_pixel_size * vec2(x, y);
			
			if ((xy != clamp(xy, vec2(0.0), vec2(1.0)) || texture(texture, xy).a == 0.0) == inside) {
				return true;
			}
		}
	}
	
	return false;
}

void fragment() {
	vec2 uv = UV;
	
	if (add_margins) {
		vec2 texture_pixel_size = vec2(1.0) / (vec2(1.0) / TEXTURE_PIXEL_SIZE + vec2(width * 2.0));
		
		uv = (uv - texture_pixel_size * width) * TEXTURE_PIXEL_SIZE / texture_pixel_size;
		
		if (uv != clamp(uv, vec2(0.0), vec2(1.0))) {
			COLOR.a = 0.0;
		} else {
			COLOR = texture(TEXTURE, uv);
		}
	} else {
		COLOR = texture(TEXTURE, uv);
		COLOR.rgb *= modulate;
	}
	
	if ((COLOR.a > 0.0) == inside && hasContraryNeighbour(uv, TEXTURE_PIXEL_SIZE, TEXTURE)) {
		COLOR.rgb = inside ? mix(COLOR.rgb , outline_color.rgb, outline_color.a) : outline_color.rgb ;
		COLOR.a += (1.0 - COLOR.a) * outline_color.a;
	}
	COLOR.a *= alpha;
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_y3fbr"]
resource_local_to_scene = true
shader = SubResource("Shader_vxi4y")
shader_parameter/outline_color = Color(0.160784, 0.160784, 0.160784, 1)
shader_parameter/modulate = Color(0.4, 0.360784, 0.341176, 1)
shader_parameter/alpha = 1.0
shader_parameter/width = 1.0
shader_parameter/pattern = 1
shader_parameter/inside = false
shader_parameter/add_margins = false

[node name="GoldCoinsPoppLabel" instance=ExtResource("1_xy2q2")]
material = SubResource("ShaderMaterial_y3fbr")
offset_bottom = 16.0
theme_override_constants/separation = 1

[node name="Label" parent="." index="0"]
text = " +7"

[node name="RightTextureRect" parent="." index="1"]
visible = true
texture = ExtResource("2_t33iy")
